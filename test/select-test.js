import React from 'react';
import {shallow} from 'enzyme';
import {expect} from 'chai';
import {noop} from 'lodash';

import { Select } from '../src';

it('Default properties');

it('Has a placeholder');

it('Has an onChange function');

it('onChange function is rendered');

it('Has a value of number');

it('Has a value of string');

it('Has a label');

it('Has a text value');

it('Has a component value');

it('is Searchable');

it('Does not have search capabilities');

it('is Open');

it('onToggleOpen is rendered');
